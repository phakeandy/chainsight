apiVersion: v1
kind: Service
metadata:
  labels:
    app: postgraphile
  name: postgraphile
spec:
  selector:
    app: postgraphile
  ports:
    - name: http
      port: 5000
      protocol: TCP
      targetPort: http
