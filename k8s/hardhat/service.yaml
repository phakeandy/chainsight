apiVersion: v1
kind: Service
metadata:
  labels:
    app: hardhat
  name: hardhat
spec:
  selector:
    app: hardhat
  ports:
    - name: rpc
      port: 8545
      protocol: TCP
      targetPort: rpc
